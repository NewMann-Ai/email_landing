import React from "react";

export default function LangSwitcher() {
    return (
        <p className="text-(--text) text-sm hover:text-(--primary) transition-colors duration-200 cursor-pointer">
            IT
        </p>
    );
}
